# Super Time 隐私声明

> 生效版本：**v3**（与 `src/client/ui-app/privacy/consent.ts` 的 `PRIVACY_VERSION` 一致）
> 适用范围：Windows x64 桌面版 Super Time
> 最近一次修订：2026-09-19（新增一个 LLM 出网用途：**知识库文件的模型摘要**。
> 点「生成摘要」时，该文件正文的**前若干字**（上限 8,000 字，按文档原有顺序取开头）会
> 发送给你配置的模型，换回一段要点并存进本机 `wechat_kb_files.db`，见第四节 A。
> 这是**数据用途的材料性变更** —— 在此之前送出去的是「检索命中的片段」与「待总结的
> 当日/周期消息」，现在还包括「你指定的一份文件的开头正文」，故 `PRIVACY_VERSION`
> 由 v2 提升至 v3，要求重新同意。关掉「参与语义检索（会出网）」的文件**一律拒绝生成摘要**。）
> 上一次修订：2026-09-19（**知识库文件正文**进入 embedding 出网范围：知识库问答会把
> 「参与 RAG」的文件块正文送去你配置的 embedding 接口建向量索引，见第四节 B。
> 这是**数据用途的材料性变更** —— 在此之前只有检索到的聊天片段会以向量形式出网，
> 现在你选进知识库的文件正文也会 —— 故 `PRIVACY_VERSION` 由 v1 提升至 v2，要求重新同意）
> 上一次修订：2026-09-15（把「自动获取原图（CDN）」「原图解密方式」两个开关落实为真实生效；
> 纯开关层面的行为收敛不改变数据用途，故未提升 `PRIVACY_VERSION`）

## 一句话结论

你的微信数据在本机解析、存储与分析，**不上传到我们的任何服务器**（我们没有任何服务器）。
唯一会把你**内容**发出去的功能是 AI 问答与总结 —— 那会把检索到的聊天片段发给你**自己配置**的
模型接口；应用里有一个开关可以整体关掉它。除此之外还有几处**不涉及聊天内容**的出网点
（头像、远程图片/视频、地图底图、语音模型下载），第四节逐条列明。

## 一、我们读什么

### 1. 本机微信数据（只读）

- 解密后的 SQLite 库：消息、联系人、群成员、朋友圈、收藏、转账记录等；
- 媒体缓存：图片、视频、语音、文件的本地副本与我们自己的解码缓存。

这些数据来自你**自己**的微信安装（`Weixin.exe`）与数据目录。应用把它们解密、
复制到自己的数据根后**只读**使用。

### 2. 运行中的微信进程内存（只读，用于取数据库密钥）

微信 4.x 的数据库密钥不以明文存在磁盘上，只在**运行中的微信进程内存**里。
因此「自动获取密钥」会：

- 通过 Win32 API（`OpenProcess` → `VirtualQueryEx` → `ReadProcessMemory`）**只读**扫描
  `Weixin.exe` 进程的内存区域；
- 只挑「已提交、可写、非 guard」的区域，按已知的密钥模式匹配并用一个数据库文件校验命中结果；
- **不写入、不修改、不注入、不 hook** 微信进程，也不把扫描到的内存内容保存或外发；
- 命中后把密钥写入本机 `<数据根>/secrets.json`（权限收紧到当前用户）。

代码：`src/backend/wechat-data/src/keys/win32-memory.ts`、`keys/service.ts`。
微信未运行时该能力不可用，可改为手动填写密钥。

### 3. 本机密钥材料与配置

图片密钥可从微信的 kvcomm 缓存（`*_input.statistic` 文件名前缀）与
`%APPDATA%\Tencent\xwechat\config\*.ini` 推导；这只读本机文件，不外发。

### 4. 设备指纹（用于授权）

授权校验需要一份设备指纹（机器标识的哈希）。它**只在本机计算**，写入你导出的
`activation-request.json`；只有你**主动**把该文件交给签发方时才离开本机。

## 二、我们不做什么

- 不登录微信、不发消息、不改动微信的任何文件或数据；
- 不写注册表、不安装驱动、不挂钩（hook）微信进程；
- 不收集遥测、不做崩溃上报、不做使用统计；
- 不把数据库、聊天记录、图片或导出物上传到我们的服务（我们没有这样的服务）；
- 不在后台偷偷调用模型：AI 调用只由你的操作触发（提问、总结、定时总结任务、
  知识库的「生成摘要 / 语义索引 / 抽取实体 / 找链接建议」四个按钮）。

## 三、数据存在哪里

| 路径 | 内容 | 保护 |
|---|---|---|
| `<userData>\wechat\config.json` | 路径配置与非敏感设置镜像（**不含**密钥） | 目录权限收紧到当前用户 |
| `<userData>\wechat\llm.json` | 模型接口地址与 **API Key**（可保存多套已配置的模型，切换时整体替换） | 同上 |
| `<userData>\wechat\logs\` | 诊断日志（出网相关内容会脱敏） | 同上 |
| `<userData>\wechat-data\` | 数据根：`decrypted\`（解密库）、`decoded_images\`（图片缓存）、检索/笔记/待办库 | 同上 |
| `<数据根>\secrets.json` | 微信解密密钥（db / image / xor / api_token） | 原子写入 + 仅当前用户可读 |

除上表之外，`<数据根>\wechat_privacy.db` 保存三类**本机元数据**：AI 出站审计（见第七节）、
导出历史（每次导出的路径与参数），以及**问答历史**（每次问答的问题、回答与引用来源，
供「微信问答 → 历史记录」回看）。三者都只存在本机、按数据根隔离，**不参与任何导出或分享**；
问答历史与导出记录都可以在界面上逐条删除或整体清空。

`<userData>` 在 Windows 上是 `%APPDATA%\Super Time`。**安装目录保持只读**：
升级或卸载不会带走你的配置，也不会把本机路径与密钥带到其他机器。

删除数据 = 删掉 `<userData>\wechat` 与 `<userData>\wechat-data`（或你在设置里指定的数据根）。
卸载程序不会自动删除它们。

## 四、出网点清单（与代码逐条对应）

### A. AI 调用：LLM chat（发的是**内容**）

| 项 | 说明 |
|---|---|
| 触发 | 微信问答、每日总结、周期总结、群总结任务，问答的「查询改写/优化」，单聊里的「**推荐回复**」，以及知识库文件详情里的「**生成摘要**」 |
| 发出内容 | 检索到的聊天片段 / 当日消息 / 待总结文本（**不是**整个数据库）；「生成摘要」时是**你指定的那一份文件的开头正文**，上限 8,000 字、按文档原有顺序取前若干块 —— 文件更长时后面部分不会发出去，摘要也只覆盖前面那段 |
| 目的地 | 你配置的 `llm.json` 里的 `apiUrl`（默认 `https://api.openai.com/v1`） |
| 开关 | ✅ 应用内「数据边界与出网 → 禁止 AI 出网」整体拦截；「敏感字段打码」发送前脱敏；<br/>✅ 文件级：关掉「参与语义检索（会出网）」的文件**直接拒绝生成摘要**，不改标志位也不发送 |
| 审计 | ✅ 每次放行都记入本机审计表（功能、次数、字符数），可导出 CSV |
| 代码 | `gateway.ts` 的 `privacyGate(...)`（`ask_wechat` / `daily_summary` / `period_summary` / `summary_task` / `ask_optimize` / `kb_file_summary` / `suggest_reply`） |

### B. AI 调用：embedding（发的是**内容**）

| 项 | 说明 |
|---|---|
| 触发 | ① 微信问答的稠密检索通道（把检索到的聊天片段向量化）；② **知识库问答**：把「参与 RAG」的文件**块正文**送去向量化，用于建/补该库的向量索引；③ 「知识库 → 文件」面板上的**「语义索引」按钮**（你主动点才发，只发当前这个库）；④ 同面板的**关键词+语义检索**：只发**你输入的那句查询词**，库内正文不外发 |
| 发出内容 | 待向量化的文本（按配置截断）：① 检索到的聊天片段；② **你选进知识库、且没有关掉「参与 RAG」开关的文件正文**；③ 同 ②（按钮触发的建库）；④ 检索词本身 |
| 目的地 | 同 A（`apiUrl` + `embedPath`，默认 `/embeddings`） |
| 开关 | ✅ 与 A 同一道闸门与脱敏；两处的**审计功能名分开记**（`ask_embed` = 聊天片段 / `kb_embed` = 知识库文件正文），这样「我把哪一类数据发出去了」在审计表里分得开。逐文件的「参与语义检索」开关关掉后，该文件**连候选都进不来**（条件写在取语料的 SQL 里，不是取回后再过滤） |
| 代码 | `gateway.ts` 的 `makeEmbedFn`（`feature` 参数）/ `privacyGate` / `buildKbVectorIndex`；消息侧 `query/retrieval/embedding.ts`；知识库侧 `query/kb-vectors.ts` |

> 未配置模型时这两条链路都不会发生：问答直接报「未配置模型」，检索退化为纯本地稀疏通道。
> 知识库侧还有一道**文件级**闸门：「参与 RAG」开关（`kb_files.include_in_rag`）。
> 关掉它的文件，正文既不会进向量索引、也不会被检索到 —— 该条件写在**取语料的 SQL** 里，
> 不是拿到结果之后再过滤（后过滤会让不许出网的文件先占满名额再被丢掉）。
> 索引是**按库**建的：在哪个库里提问，就只把那个库的正文送去 embedding。

### B2. 模型精排（把候选正文发给重排序模型）

| 项 | 说明 |
|---|---|
| 触发 | 问答检索融合出候选之后。**只在「数据配置 → AI 大模型」的重排序模型名非空时发生** —— 这个字段没有独立开关，填了就启用（留空则整段跳过，顺序由本地启发式给出） |
| 发出内容 | 你的查询词 + 头部若干条候选的**正文**（默认上限 60 条；聊天片段与知识库文件正文都算候选）。不发出答案、不发出你的身份标识 |
| 目的地 | 同 A（`rerankApiUrl` 留空则回落 `apiUrl`；路径 `rerankPath`，默认 `/rerank`） |
| 开关 | ✅ 全局「禁止 AI 出网」优先判：开着它时**一次请求都不发**，界面说的是「被拦截」而不是「模型不可用」。逐文件另有「参与语义检索」开关 —— 关掉的文件连候选都进不来。精排失败或被拦**不会让问答失败**，只是退回本地加权并在依据行写明 |
| 审计 | 功能名 `ask_rerank`（独立一列）。名字里不写 `kb_` 是因为候选既可能来自聊天记录也可能来自知识库文件 |
| 代码 | `gateway.ts` 的 `makeRerankFn`（`privacyBlocked` → `privacyGate`）；`query/retrieval/pipeline.ts` 的融合后精排阶段；`wechat-host.js` 的 `rerank()` |

> **一处必须说清的副作用**：开启「敏感字段脱敏」时，送出去的是**脱敏后的文本**。
> 脱敏是五条正则（手机号 / 身份证 / 银行卡 / 邮箱 / 口令），它不认识上下文 ——
> 文件正文里连续的 16–19 位数字（订单号、无连字符的 ISBN、时间戳串）会被整段替换成 `[银行卡]`。
> 也就是说**模型读到的字节与落库的原文不同**，而审计表只记录字符数、不记录被改写了什么。
> 这会影响精排的相关性判断（极端情况下把一条本来最相关的候选排到后面），
> 但不会把未脱敏的内容发出去。

### B3. 文档实体抽取（把文件正文发给语言模型换一批名字）

| 项 | 说明 |
|---|---|
| 触发 | **「知识库 → 本库模型」弹层里的「抽取实体」按钮**（`kb_extract`）。只有你点下去才发；不在建索引时顺带跑，也没有定时任务会碰它 |
| 发出内容 | **按文件**发：文件名 + 该文件的分块摘要串（默认上限 8,000 字，按文档原有顺序取前若干块，更长时后面部分不发、提示词里会写明「以下只是开头部分」）。一轮最多处理 20 个文件，其余要再点一次 |
| 目的地 | 同 A（本库的语言模型；没覆盖就用全局 `apiUrl`） |
| 开关 | ✅ 全局「禁止 AI 出网」优先判；✅ 逐文件「参与语义检索」关掉的文件**一次请求都不发**（判据写在取正文的 SQL 里）；✅ 没配语言模型时按钮是禁用状态并说明为什么 |
| 审计 | 功能名 `kb_extract`（独立一列） |
| 代码 | `gateway.ts` 的 `extractKbEntities`（`privacyBlocked('kb_extract'` → `makeChatAsker`）；`query/kb/extract.ts` |

> 抽出来的东西落在**新表**（`kb_doc_entities`），是**推断**而不是原文：图谱里连到它的边一律画成
> 虚线，详情里写明「模型推断 · 由 <模型名> 抽出」。重跑同一个文件会**覆盖**它上一次的结果。

### B4. 笔记的链接建议（把正在写的正文发给向量模型）

| 项 | 说明 |
|---|---|
| 触发 | 笔记编辑器里的**「🔗 找链接建议」按钮**（`kb_link_suggest`）。刻意**不**随打字触发 —— 那样会把同一句话发几十遍 |
| 发出内容 | 你正在写的**标题 + 正文**（截到 1,200 字）+ 本库的候选名字（已有笔记标题与上一步抽出的实体，最多 80 条短字符串）。不发给模型的是：别人的聊天内容、你的身份标识 |
| 目的地 | 同 B（本库的嵌入模型） |
| 开关 | ✅ 全局「禁止 AI 出网」优先判；✅ 没配嵌入模型时直接报「未配置」，一个候选都不取 |
| 审计 | 功能名 `kb_link_suggest`（独立一列） |
| 代码 | `gateway.ts` 的 `suggestKbLinks`；`query/kb/suggest.ts`（纯排序，无写路径） |

> **它不写你的笔记。** 返回的只是候选名字，变成 `[[链接]]` 的唯一途径是你在编辑器里点那枚芯片。
> 因此模型判错的代价是「你没点」，而不是「正文里多了一条你没写过的链接、还进了图谱」。

### C. 头像图片（不发内容，但会暴露你关注了谁）

| 项 | 说明 |
|---|---|
| 触发 | 通讯录/聊天里显示头像且本地没缓存（实测约 82% 的联系人本机无缓存） |
| 目的地 | 微信记录的头像地址：`wx.qlogo.cn`、`mmhead.c2c.wechat.com` 等腾讯图片 CDN |
| 开关 | ❌ 应用内没有开关（登记在 `docs/RELEASE-PLAN.md`） |
| 代码 | `query/avatar.ts` 解析本机头像缓存与微信记录的头像地址；渲染进程按该地址直接加载（CSP 的 `img-src https:` 允许任意 https 主机，见 `src/client/ui-app/index.html`） |

### D. 远程图片 / 视频 / 公众号封面（不发内容）

| 项 | 说明 |
|---|---|
| 触发 | 表情原图、**聊天图片的原图**（你在图片下方点「取原图」才会发请求，不会自动批量取）、朋友圈视频与封面、公众号文章封面在本机没有缓存时，后端按消息里的地址取回 |
| 目的地 | 微信 CDN（消息 XML 里的 `cdnurl` / `<url>` / `<thumb>`；聊天图片走 `tpurl`/`tphdurl` 直链，**主机白名单固定为** `qq.com`、`weixin.qq.com`、`wechat.com`、`wechatcdn.cn`、`qpic.cn`），公众号文章域名 `mp.weixin.qq.com` |
| 开关 | ✅ **「自动获取原图（CDN）」**：关闭后后端**不发起任何取回请求**，只用本机缓存（已有缓存照常可用）；<br/>✅ 另有「原图解密方式」：`本地解密`（默认）表示对 CDN 的加密流用 `<enc key>` 本地解密，`服务端解密`表示直接用远端字节；<br/>✅ 隐私面板的「禁止 AI 出网」也会拦下朋友圈封面、视频**与聊天图片原图**的取回；<br/>✅ 聊天图片原图另有一道**域名白名单**：链接主机不在上面五个后缀内就拒绝发请求（消息 XML 是可被离线改写的文件，不能让它指定我们去哪儿） |
| 记录 | 取回成功/失败会写进**操作记录**（功能名 `sns_cover_fetch` / `sns_video_fetch` / `image_original_fetch`），被「禁止 AI 出网」拦下时原因直接回给界面。注意这些**不进** AI 的那张隐私审计表 —— 它们不是模型调用，审计表按「哪一类数据出了网」分列，混在一起就会把「我把消息正文发给了谁」这条查询问成一个含糊的数 |
| 代码 | `query/cdn-policy.ts`（开关口径的唯一出处）、`query/media-image.ts`、`query/image-original.ts`（聊天图片原图：直链解析 + 白名单 + 落盘）、`query/sns-video.ts`、`query/article-cover.ts`；开关由 `gateway.ts` 的 `cdnSwitches()` 读入并传进取图路径 |

> **这条能力的边界（实测，不粉饰）**：本机 39,923 张图片消息里只有约 **16%** 带着免登录的原图直链，
> 所以「取原图」按钮对另外约 82% 会明确回一句「这条没有免登录直链，请在微信里点开一次」。
> 那部分指针（`cdnbigimgurl`）技术上要的是**微信登录态凭据**（会话密钥 / 设备签名），
> 也就是「以你的身份向微信服务器发请求」—— 本应用**不做**，也不会去做：它和本文开头
> 「不登录、不发送消息、不连微信服务器同步聊天记录」是同一条承诺的两端。

### E. 地图底图（不含你的数据）

| 项 | 说明 |
|---|---|
| 触发 | 打开「世界地图 / 中国地图 / 省市地图」时拉取行政区划 GeoJSON |
| 目的地 | `cdn.jsdelivr.net`、`unpkg.com`、`geo.datav.aliyun.com` |
| 开关 | ❌ 无（离线时回退到内置的简化边界） |
| 代码 | `panels/world-map-data.ts`、`ChinaMap.tsx`、`province-map-data.ts`、`city-map-data.ts`；CSP 白名单在 `src/client/ui-app/index.html` |

### F. 语音模型下载（只在你要用时）

| 项 | 说明 |
|---|---|
| 触发 | 你在设置里点击「下载 whisper 引擎 / 模型」（默认不下载） |
| 目的地 | `huggingface.co`、`hf-mirror.com`、`github.com` releases |
| 开关 | 不点就不会发生；已下载的引擎与模型随包分发或留在本机 |
| 代码 | `query/whisper.ts` |

### G. 不会出网的

- **模型列表**（设置 → 模型）来自本机设置与内置目录，不发请求；
- 搜索、统计、导出、图谱、账本、备份、隐私体检、待办提取（本地关键词匹配）全部纯本地。

## 五、如何关闭出网

**关掉 AI 出网**：`数据配置 → 数据边界与出网 → 禁止 AI 出网`。
开启后问答 / 每日总结 / 周期总结 / 群总结任务一律不调用模型（embedding 也一并拦下），
并额外拦下朋友圈封面与视频的 CDN 取回；被拦下的调用**不计入**审计，界面会提示被拦截原因。

**关掉远端取原图**：`数据配置 → 设置 → 图片解码 → 自动获取原图（CDN）→ 关闭`。
关闭后后端不再向微信 CDN 取表情原图、公众号封面与朋友圈视频/封面，只用本机缓存
（已经缓存过的照常显示）。这一条与 AI 开关相互独立。

**彻底离线**：不配置模型 + 关闭 CDN 取图 + 断开网络（或在防火墙里禁止 Super Time 出站）。
此时除「头像、聊天图片（渲染层按消息里的地址直连）」与「地图底图」会失败降级外，
其余功能照常可用。

> 诚实说明：**头像**（C）与**地图底图**（E）目前仍无内置开关，语音模型下载（F）靠「不点」来避免；
> 渲染层按消息里记录的地址直连加载的图片/头像也不受「自动获取原图（CDN）」控制
> （那个开关管的是**后端主动取回**）。要完全离线请配合系统防火墙或断网。

**针对性关闭**：把 `llm.json` 的 `apiUrl` 指向你自己的内网/本地模型（例如自建的
OpenAI 兼容端点或本机推理服务），内容就不会离开你的网络。

> 诚实说明：除 AI 闸门外，C–F 目前**没有**内置开关，这是已登记的遗留项
> （`docs/RELEASE-PLAN.md`）。在它们补齐之前，请用系统防火墙或断网实现完全离线。

## 六、发送前的敏感字段打码

`数据边界与出网 → 敏感字段打码` 开启后，出站文本在发送前会把手机号、身份证号、
银行卡号、邮箱、口令等模式替换为掩码（`src/backend/wechat-data/src/query/privacy.ts`）。
它作用于 A、B 两条 AI 链路。

注意：打码是**模式匹配**，不保证覆盖所有敏感形式；对内容最敏感的场景请直接关闭 AI 出网。

## 七、AI 出站审计

`数据边界与出网 → AI 出站审计` 记录**被放行**的每次调用：功能名、次数、字符数、会话与消息数。
可导出 CSV、可清空。审计只存在本机数据库里（`wechat_privacy.db`），不外发。
被「禁止 AI 出网」拦下的调用不计入。

## 八、你的控制权

| 你可以 | 怎么做 |
|---|---|
| 关掉 AI 出网 | 应用内开关（见第五节） |
| 换模型 / 换服务商 | 设置 → 模型配置（`llm.json`） |
| 切换已保存的模型 | 「数据配置 → AI 大模型」顶部的「已配置模型」芯片：点击即切换；也可逐条删除（其余配置不受影响） |
| 不提供密钥 | 不点「自动获取密钥」，改用手动填写；或在设置里清空 |
| 导出你的数据 | 导出（CSV/HTML/TXT/…）、备份管家 |
| 删除问答历史 / 导出记录 | 「微信问答 → 历史记录」里逐条删除或整体清空；「导出记录」里删除或按策略清理 |
| 删除全部数据 | 删除 `<userData>\wechat`（含密钥与日志）与数据根目录 |
| 重看 / 重新同意本声明 | 主界面「数据配置 → 高级设置 → 重新查看启动页」 |

## 九、变更

本声明随版本变更；材料性变更（新增出网点、改变数据用途、扩大扫描范围）会提升
`PRIVACY_VERSION`，并要求你在下次启动时重新同意。文档与代码的对应关系由
`src/backend/tests/privacy-statement.spec.ts` 守住（出网点清单必须与代码里的实际调用点一致）。

---

**相关文档**：[`README.md`](../README.md) · [`docs/API.md`](API.md) ·
[`docs/RELEASE-PLAN.md`](RELEASE-PLAN.md)（已知缺陷与遗留项）·
[`src/backend/README.md`](../src/backend/README.md)（密钥存储与保护）
