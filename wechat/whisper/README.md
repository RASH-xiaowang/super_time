# Whisper 模型目录（默认）

【数据配置】→「语音转写」中的“模型目录”留空时，默认使用本目录：

```
D:\super-time-wechat\wechat\whisper
```

- 安装的 whisper.cpp 引擎位于 `wechat/whisper/bin/whisper-cli.exe`
- 下载的 ggml 模型（ggml-tiny.bin / ggml-base.bin / ggml-medium.bin …）也保存在此目录
