# 变更记录

本文件记录所有**对外可见**的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 版本策略

- **唯一版本来源**是 `package.json` 的 `version`；`main.js` 读它作为 `APP_VERSION`，
  授权校验与安装器都用同一个值。
- 每次发布：先改 `package.json` → 在本文件顶部补一段 → `npm run dist` 出安装器。
- 材料性变更（新增出网点、扩大数据读取/扫描范围、改变数据用途）除了升版本，
  还要提升 `src/client/ui-app/privacy/consent.ts` 的 `PRIVACY_VERSION`，
  让用户在下次启动时重新同意 [`docs/PRIVACY.md`](docs/PRIVACY.md)。
- 逐条实施状态与验收证据不在本文件里，在 [`docs/RELEASE-PLAN.md`](docs/RELEASE-PLAN.md)。

## [1.0.4] - 2026-09-17

### 新增

- 重构应用导航外壳与主要数据面板，统一页面标题、工具栏、列表、加载态和空状态体验。
- 新增全局搜索、地区概览、导出历史对话框和统一的二次确认组件。
- 「微信问答」新增问答历史记录：每次问答自动保存完整记录（问题 / 回答 / 引用 / 检索元信息），
  面板头的「历史记录」按钮可搜索、按状态与来源筛选、展开回看并逐条删除或整体清空；
  记录只存本机、按数据根隔离，不参与导出或分享。
- 「数据配置 → AI 大模型」新增「已配置模型」快速切换：成套保存多份模型配置（供应商 / 模型 /
  地址 / API Key / 向量模型），点一下即切换、下一次提问立即生效，无需重启；支持新增、改名与逐条删除。
  同时修正「套用厂商模板保留旧 API Key」导致换厂商后必然鉴权失败的问题（换厂商时清空 Key 并明确提示）。
  老配置自动迁移为第一条记录，`llm.json` 的顶层字段仍是「当前生效值」，旧版本读取不受影响。
- 导出链路新增历史记录能力，并提供 CSV 导出与历史查询回归测试。
- 增加 UI 端到端冒烟、导航审计、总览布局审计与面板审计脚本。
- 社交图谱新增「同备注编号」关系层：备注形如「宜州一中404陈泳达」的联系人按「前缀 + 编号」
  归为同一批（班级 / 批次），在图上用**金色虚线**连成一片，图例与画布同款虚线以自解释
  「这是从备注结构推断的、不是微信数据里观测到的共处」；详情面板新增「同班级」区块，
  列出该批的全库成员及「全库 43 人 · 本视图 12 人」的对比 —— 被「仅显示好友」或节点上限
  挡在图外的人压暗且不可点，这正是此前「备注前缀相同却没有连线」的答案。
  为避免完全图吃掉连线预算，每批按「锚点串链 + 其余轮转挂载」生成 `n−1` 条边
  （43 人班 = 42 条，而不是 C(43,2)=903 条）；两侧都可在「同备注编号连边」开关处关掉。
- 群组网络每个群的共同成员上限由 8 人放宽到 200 人。真机实测（64 个群 / 2541 条成员条目）：
  旧上限下 **38 个群的成员关系被截断**（只带出 439 条），这正是「群与群之间看不到共同成员」
  的来源；放宽后只剩 3 个群被截断（带出 2115 条，约 98KB），社区检测也拿到了完整的成员结构。
- 「知识库」新增**文件视图**（面板第三段「文件」）：把本机文件登记进某个知识库，逐条显示
  解析状态、分块数、大小、登记时间与「是否参与语义检索」，可单个移除、可按状态筛选。
  **登记只读取、只上传内容副本，绝不改动或删除你电脑上的原文件**（移除与删库同理）——
  界面文案与后端行为都按这条写死，原文件改名 / 移动 / 删掉都不影响已入库的内容。
  解析状态分**八档且不合并**，其中「本机还没有这类文件的解析器」（琥珀色）与
  「解析这个文件时出错了」（红色）是**两句不同的话**，绝不把「没有解析器」说成「文件损坏」；
  关掉某个文件的出网开关后**关键词检索仍然命中它**（界面上就这么写，避免用户以为关掉等于删掉）。
  内容进入知识库后，才谈得上被后续的检索能力用上。

- 「知识库」的**文件解析器**落地：PDF / Word（`.docx`）/ Excel（`.xlsx`）现在会**真的抽出正文**进库，
  关键词检索能搜到里面的字（此前这三类登记进去只能按文件名搜到）。解析**搬到后台队列**：
  登记立刻返回，文件先显示「排队中」再自行推进到「可检索」，解析大文件期间不会把界面卡住
  （列出文件与查询接口照常响应）。三个格式各自的取舍都写进界面与文档：PDF **逐页**抽取且**带上页码**
  （检索结果能说清命中在第几页）、Word 把**标题层级**带进分块面包屑、Excel **每个工作表都进**
  且每块**重复表头**（表格被切成多块时，第二块起也看得到列名，不会变成一堆没头没脑的数字）。
  仍然不支持的类型照旧**如实**说「本机还没有这类文件的解析器」，绝不改口成「文件损坏」。
  三个解析库**全部内置**在安装包里（装完即用、不联网下载），代价是后端打包体积从约 24 MB 增到约 56 MB。
  附带修掉一处会让**中文 PDF 整页空白**的问题：使用预定义 CID 字符表（`UniGB-UCS2-H` 一类，中文 PDF 里很常见）的文件会**一个字都抽不出来**，并被误报成「多半是扫描件或纯图片」——而这类文件其实有完整的文字层。根因是解析时没有接上解析器自带的 CID 字符表；修复后同一份文件由 `unsupported` 变为 `ready`，正文与页码都正确。

### 修复

- 统一品牌显示与导航图标，修复页面迁移后在导航、深链和状态恢复上的一致性。
- 修正文件、账本、朋友圈、隐私、总结等页面的加载状态、分页范围和图表展示细节。
- 强化后端进程、控制台安全与文件权限处理，补充联系人及导出相关测试。
- 社交图谱「同一群组内成员之间没有双向链接」：**建边没坏，是画的时候被裁掉了**。真机实测
  （251 节点 / 998 边）：同群关系共 748 条且 748/748 都带双向标记，与按 `group_codes` 独立推出的
  关系逐条相等（缺 0、多 0）；但连线额度是「0.9 条/节点」，只放得下 226 条 —— **251 个人里 103 个
  的同群伙伴在画面上一条线都没有**。额度改为「5 条/节点」（实测默认视图是 3.98 条/节点）并加
  24000 条的绝对上限：默认规模下 748 条同群关系一条不落，而 10.63 条/节点的群组网络仍按权重截断。
  想主动收窄同群关系用面板上的「共同群阈值 ≥」，而不是靠隐形的预算。
- 社交图谱「部分节点缺少连线」：连线预算按类型分桶后，度数低的节点会**成片失去唯一的那条边**。
  真机实测（251 节点 / 998 条边）：预算 226，250 条「我→对方」的辐条被截到 113 条，
  137 个节点在画面上一条线都没有。现在每个有边的节点保底保留一条最强关联边
  （连通性骨架，不参与预算竞争），实测丢失节点 137 → 0，且仍不是「全画」（预算继续生效）。
- 「知识库」的文件解析偶发「排队之后直接失败」：列表显示解析出错、分块数为 0，而文件本身完全正常。
  真实原因是知识库的文件库不是 WAL 模式，解析成果要写进去时必须拿到排他锁，**恰好被另一个读操作挡一下
  就会把这一次写入整份丢掉** —— 解析其实已经成功，只是最后一步写库失败就被判成「解析出错」。
  这个失败是**暂态**的（重试就好），现在这一步会自动重试几次（总退避约 0.4 秒），不再把一次撞锁
  当成解析失败；确实读不动的文件（内容被截断等）仍照样判失败，不会被重试掩盖。
  修完真机四份真实样本（PDF / 中文 PDF / Word / Excel）一次跑通，四个文件的正文、页码、
  标题层级与表头都能检索到。

### 调整

- 应用描述统一改为 Super Time，补充 `jsdom` 与 UI 审计工具链。
- 「社交图谱」并入「通讯录」导航项：两者本就是**同一批人的两种看法**（联系人列表 / 关系图），
  现在由面板顶部的分段条在同一屏内切换，不再各占一条侧栏条目（侧栏 17 → 16 项）。
  图谱面板内部功能零改动（画布 / 图例 / 右侧控制栏 / 海报与 SVG·PNG 导出全部保留），
  `#graph` 深链与各面板的跨页跳转照旧可用；「知识图谱」是「我的笔记」，不属于通讯录语义，
  因此仍是独立入口。顺带补齐小屏适配：图谱的固定宽度控制栏在 ≤900px 改为纵向堆叠
  （原先它不让位，窄窗口下会把画布压到节点重叠），通讯录的 A–Z 索引栏在 ≤700px 隐藏
  （窄屏列表退化为单列后，它会压住联系人卡片；搜索是等价路径）。
- 启动引导页版本号更新为与安装包一致的 `1.0.4`。
- 「每日总结与任务 / 周期总结 / 年度报告」三页按同一套紧凑密度刻度重排：能一行放下的元素
  不再拆成两三行（任务/记录行、周期总结的表单与统计元信息、年度报告的键值格与「还有这些人」小卡），
  间距与字号统一收敛到 10/11/12px 三档并压缩内边距。任务行行高由约 74px 降到约 27px，
  同屏可见条数约为原来的 2.7 倍；年度报告的海报网格与退化规则保持不变。
- 合并页顶部那条分段切换（消息视图 / 收藏与表情 / 文件与存储 / 资金往来 / 群聊分析 /
  总结与报告，共 6 组）同步收紧：去掉条上方 14px 的纯留白，条与面板之间的留白由
  30px 收到 14px；子面板的上下内边距由外壳统一覆写收一档，不再各页一个值；
  分段项的横向内边距 12px → 10px 以减少窄窗口下的换行（最小点击高度仍保持 24px）。
- 社交图谱布局重做：间距归一由「只放大」改成**双向**，密度统一按节点半径表达。
  群组网络的「最近邻距离 / 中位半径」由 7.61 收到 3.40（与好友网络的 3.44 对齐），
  包围盒面积 444k → 94k、面积填充率 2.9% → 13.9%（同一份真实数据的实测值）；
  压缩带来的相交由碰撞松弛兜底（残留相交对 0），并把布局落定后的松弛轮数由 4 提到 24
  ——实测 4 轮会残留 3 处相交、8 轮起为 0。
- 「一条边都没有」的节点不再参与力导向，改为排在主簇下方的确定性网格区：
  它们没有弹簧可收敛、留在仿真里只会被斥力甩远把取景框撑大；现在位置只由半径 / 间距 /
  主簇包围盒决定，不随六个力度滑杆洗牌，槽距按最大半径算因而彼此不重叠。
- 全应用 15 个搜索框的**搜索范围**核对与修正（此前多处「搜不到」不是数据里没有，
  而是过滤只作用在已加载的那批数据上）：
  - **文件资产**：搜索改为服务端过滤，覆盖由「最近 500 条 / 共 4305 条（11.6%）」变成全量；
    并修复「按 MD5 搜索」—— 原先写成 `(fileName || md5)`，`||` 短路让 md5 永远不参与比较，
    即这个功能从来没生效过。分类计数（图片 / 视频 / 文件）现在随搜索一起变化。
  - **操作日志**：后端新增关键词条件，覆盖由「最新 500 条 / 共 2144 条（23.3%）」变成全量
    （此前后端根本没有关键词参数，只能本地过滤已加载的那一页）。
  - **我的收藏 / 撤回消息**：同样改为服务端过滤，不再受「一次拉 500 条」的上限限制。
  - **视频号页签**：修复「总数显示 2 条却一行都不显示」—— 该表的 `finder_live_id` 是
    超出 JS 安全整数范围的 64 位整数（实测 1214293303336576460 / 2078956456102183056），
    读取时抛的异常被 `catch { return [] }` 静默吞掉；现在按 `CAST(... AS TEXT)` 读取，
    并把那个「吞异常」改成显式告警。
  - **聊天左栏会话搜索**：后端关键词补上**消息摘要**，此前只比会话名与用户名，
    导致前端那条「按最后一条消息搜」的分支永远命中不了（实测搜「2026」后端返回 0、
    前端本可命中 11 条）。
  - **表情**：搜索现在也过滤**表情包名称**（此前只过滤自定义表情，在「表情包」页签下
    输入任何内容界面都无变化），搜不到时给出空态。
  - **全局搜索**：字段面与各面板对齐 —— 朋友圈可按作者名命中、文件可按 MD5 命中、
    记录可按转账单号 / 发红包人命中；点击「联系人 / 收藏 / 文件 / 记录」的命中结果时，
    会把关键词一起带进目标面板（此前只切页签，用户得重新输一遍）。
  - **转账红包**：关键词恰好是「转账 / 红包」这类数据源名时，后端仍不做筛选，
    但界面现在会说明原因，不再表现成「搜索坏了」。
  - **社交图谱**：节点搜索框标注「仅搜索当前图谱，最多 8 个候选」。
- 面板主题订阅收敛为 `useThemeMode()`：原先四个面板各自 `useSyncExternalStore` 而漏了
  `getServerSnapshot`，导致它们**完全无法服务端渲染**（React 直接抛错）。补上后
  `scripts/ui-ask-smoke` 才能覆盖社交图谱 / 知识图谱的渲染期崩溃（冒烟 32 → 34 项）。
- 切换深浅主题的动效改为**全页颜色过渡**，并把观感契约写死：触发条件（`setThemeMode` /
  `toggleThemeMode` 被调用且目标模式 ≠ 当前模式，一次切换只触发一次）、时长 **400ms**
  （用户开了「减少动效」时 **120ms**）、缓动 `cubic-bezier(0.22,1,.36,1)`（先快后慢）、
  范围（`<html>` 上登记为可插值颜色的**主题令牌**，覆盖由令牌取值的一切颜色 ——
  `background-color` / `color` / `border-color` / `outline-color` / `fill` / `stroke` 等，
  作用于**所有引用这些令牌的元素与伪元素**；不含 `background-image`（两个渐变无法插值）、
  不含阴影令牌（值是阴影简写而非颜色）、不含 `transform` / `opacity`）。
  实现只有一条：切换期间给 `<html>` 挂一次性的 `.theme-transition` 类，`scifi-theme.css` 里
  那条规则让 **93 个颜色令牌**走同一组时长与缓动，400ms + 80ms 后摘掉；
  **不在 JS 里逐元素写行内样式** —— 那样既要枚举元素（`createPortal` 出去的浮层、画布旁的
  工具条很容易漏），又会把行内样式永久留在 DOM 上。
  真机实测（1440×900、可见窗口）：切换过程中 `body` 背景色在 318 个采样点里采到 **22 种不同颜色**，
  是逐帧连续插值；**改之前同一套探针只采到 2 种颜色**（旧色与新色各占一半）—— 即用户反馈的
  「背景色切换是瞬时跳变、没有平滑过渡」。
- 上面这条过渡**从「元素级」重做成「令牌级」**：第一版给文档里每个元素挂 `background-color,
  color, …` 过渡（`*` 连 `::before` / `::after` 一起）。机制上能跑，但真机量化发现成本几乎全在
  `color` 上 —— 它是**继承属性**，于是文档里 **880 个元素各建一条**过渡、逐帧重算样式：
  一次切换累计卡顿 **265ms**（10 帧超过 16.7ms），400ms 的过渡只渲染出 **7 个色阶**，
  观感就是「一边变色一边掉帧」。
  现在改为把颜色令牌注册成可插值类型（`@property … syntax:'<color>'`），只让 `:root` 上这 93 个
  令牌动画 —— 颜色在使用时才从 `var()` 取值，令牌逐帧插值后所有引用它的元素**自动**逐帧拿到
  中间色，不需要元素自己挂过渡。过渡对象 **1461 → 93**，累计卡顿 **265ms → 40ms 上下**，
  色阶 **7 → 22**：既更平滑也更省，且因为**根本没有任何选择器**，伪元素与 `createPortal`
  浮层不可能漏。判据与防呆见 `scifi-theme.css` 那节注释。
- **撤掉上一版的 View Transitions 圆形扩散**。它逐帧量下来是像素级正确的，但恰好不满足
  「平滑的颜色过渡」，并且有三种**静默**退化成「没有过渡」的形态（都不报错、都不留日志）：
  ① 它本来就不插值颜色 —— 新旧两张整页快照之间是一条**硬边圆盘**，圆内是真新色、圆外是真旧色，
  用户看到的仍是一次跳变，只是跳变的位置被推着走；
  ② **窗口不可见时 Chromium 会整段跳过 View Transition**：`document.visibilityState` 为 `hidden`
  （窗口被完全遮挡 / 最小化）时 `transition.ready` 直接被拒
  （`InvalidStateError: Transition was aborted because of invalid state`），而主题已经在更新回调里
  提交了 → 动画不播、主题已换。本机实测反复落到这一路（`working/check-output/cdp-theme-diag2.txt`）；
  ③ 「减少动效」时旧实现是**直接切换、不做任何过渡**，于是 Windows 里关掉「动画效果」的用户
  **永远**看不到过渡 —— 而「无过渡」正是要修的缺陷本身。
  颜色过渡没有这些前提：它是普通 CSS 过渡，只要样式引擎在算样式就会插值，与窗口可见性、
  动效偏好都无关。相应地降级路径从「无动画」改成「更短的动画」：整页底色瞬间翻转恰是减少动效
  用户最难受的一下，120ms 的渐变既尊重了偏好，也不制造硬跳。
  回归护栏见 `theme.spec.ts`：CSS 里的时长/缓动必须与 TS 常量逐条相等、过渡类必须先挂后改令牌、
  令牌登记清单**双向**比对（漏登记 → 静默不渐变；误登记成非颜色令牌 → 字体/阴影整块失效）、
  且**禁止** `startViewTransition` / `clipPath` / `::view-transition` 再被写回来。
- 「知识库」侧栏入口新增第三段「文件」（与「笔记库」「知识图谱」同一面板、同一分段条，
  由面板顶部段条切换），三段各自的子面板内部零改动。侧栏入口数仍 **16**（并入式合并不新增
  侧栏条目，这是既有约定）；可路由页签 **36 → 37**（`kbfiles` 是「隐藏但可路由」，`#kbfiles`
  深链可用）；Remote 方法数 **145 → 149**（`getKbFiles` / `addKbFiles` / `deleteKbFile` /
  `setKbFileRag`）。`src/client/README.md` 的标题行、启动引导页 rail 的 `REMOTE API` 计数
  与 `docs/API.md` 已同步，其中前两者由文本读的用例守住（`src/client/ui-app/**` 不在
  `typecheck:client` 的 include 内，只能靠用例守）。
- 「知识库」新增**文件内容的检索**（FTS5 稀疏通道）：文件视图的搜索框输入一个词就能检索库内文件正文，
  结果区标题写明实际走的是哪条通道与用时，命中项带得分与关键词名次、正文命中处高亮；左侧列表保留
  「文件名命中」与「正文命中」两类结果，两句话不互相打脸。关键词与结果按库隔离 —— 切库即清空，
  在另一个库里搜同一个词得到 0 条。与问答侧同一约定：**不给模式开关**，走哪条通道由系统决定。
- 修复「删除知识库时不数文件」的问题。库内登记的文件数此前没有并入库列表（文件记在另一个库文件里），
  于是一个**只装了文件、笔记一条没写**的库会被删库弹层说成「这个库里没有笔记也没有文件」，
  点「一并删除」把正文分块、关键词索引、向量与 blob 副本一起清掉，**却没有任何回执**，
  也**不给「移到别的库」这条路** —— 唯一出口只剩彻底删除。现在弹层与危险按钮都会写出
  「N 条笔记 / M 个文件」（含库里当时的真实登记数），删库后回执逐项报出清掉或迁移的数量，
  并覆盖设计稿要求的「N 个文件与目标库内容重复，未迁移」；文件侧没能清点成时会明说
  「文件侧没清点成，请到「文件」分段核对」，而不是把「没清点」含糊地说成「0 个」。

## [1.0.3] - 2026-09-16

### 新增

- 问答新增回答接地核对：金额、日期、手机号/卡号等必须来自引用原文，发现不一致会带具体问题回炉重写。
- 问答规划加入本地联系人、群名与会话标题的确定性实体识别，明确点名的人不再完全依赖模型规划。
- 知识图谱画布改为自研 Canvas2D 渲染与后台布局，替换 ECharts 图谱画布；支持新的物理布局、拖拽和连接展示。
- 批量头像接口支持本地头像优先、联系人 URL 兜底，并遵守出站屏蔽设置。

### 修复

- 会话内 AI 问答不再召回该用户在其它会话中的发言；日期随消息行下发，跨天窗口不再靠模型猜日期。
- 金额与数字出处按原文位置校验，日期或时钟中的数字不再被误当成金额来源。
- 规划器关键词保留连续短语，减少跨词 bigram 噪音，提高转账等关键词的检索排序精度。
- 修正启动引导页仍写死的 `1.0.2` 版本显示，改为与安装包一致的 `1.0.3`。

### 调整

- 知识图谱、设置、操作日志和首次配置页面继续优化布局与交互；补充图谱、导航、头像和问答接地相关回归测试。
- 新增 `graphology` 与 `graphology-layout-forceatlas2` 依赖并获得内联打包授权。

## [1.0.2] - 2026-09-15

### 修复

- 修复在启动引导的授权验证步骤导入许可证后，点击「进入系统」没有反应、必须重启应用的问题；完成后会立即刷新授权状态并由原有授权门禁兜底。
- 修正启动引导页仍显示 `1.0.0` 的写死版本号，改为与安装包一致的 `1.0.2`。

### 调整

- 更新与许可证到期提醒现在覆盖启动引导、授权解锁和隐私同意页面；停留在首启流程中也能及时看到新版本。
- 在没有设置弹窗的首启页面移除无效的「去软件授权」动作，保留仍可用的导出激活请求等操作。

## [1.0.1] - 2026-09-15

### 新增

- 新增首次配置向导：把账号检测、数据库密钥、图片密钥与图片解码收成 5 步，并支持从第一个未完成步骤继续配置。
- AI 模型目录新增 MiMo（小米 Token Plan），支持已确认的 `mimo-v2.5` 模型。

### 调整

- 重构设置弹窗与旁栏导航：按配置、智能与隐私、授权与更新、维护自检、高级分组，降低首次使用和理解门槛。
- 将隐私体检与操作日志保留为主界面数据视图，配置、授权和维护动作继续收在设置弹窗中。
- 更新界面细节、健康页与提醒卡片样式，并同步调整 UI 验收、SSR 冒烟和前端说明。
- 轮换客户端许可证公钥，配合新的许可证签发材料。

## [Unreleased]

### 说明：尚不具备公开发布条件

上线门禁（`docs/RELEASE-PLAN.md` 第十二节）仍有未闭环项，**明确阻塞公开发布**的有：

- 随包资产许可未明（微信表情原图、朋友圈视频解密 WASM）——待法务结论；
- 历史提交中的真实密钥需轮换并重写 git 历史（仓库侧已出库，人工动作待执行）；
- 尚无出网总闸门：AI 之外的出网点（头像、远程图片/视频、地图底图、语音模型下载）目前无内置开关。

### 新增

- 本机微信数据工作台：会话与消息检索、全库统一搜索、统计与总览、联系人 360°、群聊洞察、
  朋友圈洞察、资金账本、数据健康、备份管家、隐私体检与敏感信息扫描、导出（CSV/HTML/TXT/XLSX/ZIP）。
- AI 能力：微信问答（稀疏 + 稠密检索）、每日总结、周期总结、群总结任务，
  统一经过隐私闸门（出站拦截 / 敏感字段打码 / 出站审计）。
- 社交图谱与知识图谱两个并列入口；知识笔记（`[[链接]]`、未解析目标成占位节点）与问答沉淀。
- 授权体系：设备指纹 + Ed25519 签名许可证，未授权时业务方法一律拒绝。
- 可靠性：后端进程独立运行并带超时与崩溃自愈（指数退避重启 ≤3 次）、
  导出与检索的流式化（内存有上界）、诊断日志落盘与导出。
- 安全基线：渲染进程沙箱、`shell.openExternal` 白名单（仅 http(s)、拒绝带凭据 URL）、
  导航守卫、CSP 收紧、asar 完整性等 fuses、打包态剥离调试启动开关。
- 工程化：vitest 单测 + windows-latest CI（含 bundle 与类型声明的一致性门禁）、
  打包冒烟、多条安全/功能冒烟脚本。
- 文档：根 `README.md`、`LICENSE`（MIT）、本变更记录、[`docs/PRIVACY.md`](docs/PRIVACY.md)、
  由 `gateway.ts` 自动生成的 [`docs/API.md`](docs/API.md)。

### 修复（相对首轮审查，见 RELEASE-PLAN 的条目）

- 授权闸门异常时改为**拒绝**而非放行（原先许可文件损坏会无证放行）。
- 后端调用加超时、worker 崩溃自动重启（原先卡死会无限转圈、进程死掉后功能整体失效）。
- 导出与搜索不再全表物化（原先最坏情况常驻数 GB 内存并阻塞事件循环）。
- 配置写入改为原子写，损坏文件先备份再覆盖（原先会被默认值无声覆盖）。
- 密钥不再镜像进 `config.json`，改存权限收紧的 `secrets.json`；开发态迁移不再携带数据源与密钥
  （原先会把真实微信库静默解密到新的状态目录）。
- 打包版语音 silk 解码不可用（可执行文件落在 asar 内无法 spawn）。
- 干净检出打包会打出没有前端的包（`pack`/`dist` 现前置 `build:ui`）。
- 主进程未声明 `AppUserModelID`，任务栏固定/分组可能认成两个应用。
- 「自动获取原图（CDN）」与「原图解密方式」两个开关此前只存不用（关掉后取图路径照旧出网，
  界面上却显示「已关闭」）；现在关闭后后端不再发起取回请求，且与 AI 出网开关相互独立。
- 设置里的「HTTP API 启用 / 访问令牌 / 监听端口」三个开关没有对应服务端，已从界面撤下
  （避免让用户以为关掉端口就等于关掉了外部访问）。
- 向量索引构建不再与并发调用互相撞锁：写事务不再跨网络保持；同一向量库上的并发调用会合并到
  同一轮构建；重建中途失败也不会把已有向量清空（删除动作随事务一起回滚）。
- 建向量索引与若干统计面板不再把整张消息表读进内存（改为逐行游标读取）：
  20 万行语料下构建期内存峰值从 +340MB 降到 +103MB，且余量只随「单次构建上限」变化。
- 一次性数据源引导（bootstrap）不再只拷 SQLite 主库：现在连 `-wal` 一起拷（跳过 `-shm`），
  避免在 WAL 模式下把「上一代」索引静默拷到新数据根。
- 成员搜索与索引重建不再互相撞锁：索引库的写入统一走一个写闸，构建在飞时显式降级并留痕。
- LLM 之外的出网点（公众号封面、图片、朋友圈视频、whisper 引擎/模型下载）也走重试；
  模型下载支持断点续传，并修掉「截断的下载被当成完整模型」这个缺陷。
- 向量索引状态查询不再每次重开连接并 `COUNT(*)`（改为指纹缓存 + `meta.rows`，热路径 0 SQL）；
  每日摘要的到期任务改为有界并发；顺带修掉「从未手动运行过的任务永不触发」的调度缺陷。
- 打包内容改为白名单 + 体积预算（`src/` 下只允许已知子树），构建日志里的体积统计不再恒报 0KB。
- 一批前端正确性与性能修正：反馈按钮的并发闸门、快照监听者 Map 泄漏、分页 `hasMore` 判据、
  图谱小地图的逐帧重算、世界地图的 memo、朋友圈文本解析的平方项（2.9MB 语料 12.97s → 2.88ms）。
- 测试与脚本不再写仓库：`license-smoke` 改为注入测试公钥，视觉审计脚本收回 `tools/visual-audit/`
  （并从源码里移除硬编码的真实群名）。
- 导出与备份改为条目级流式写入：xlsx 导出 10 万行的内存峰值从 +221.6MB 降到 +26.5MB，
  导出过程支持取消（取消后不留半成品），备份在部分失败时如实报错而不是「报成功」。
- 图片列表取图改为批量查询：一屏 30 张图只查一次库（原先逐张 30 次）。
- 反馈提交加了去重（同一轮 10 秒内重复提交只生效一次）。
- 首启的三道闸门（引导/授权/隐私同意）新增**仅非打包态生效**的调试豁免开关，
  供 UI 自动化使用；打包版即便设了环境变量也不会被豁免。
- 「提示语 N 秒后消失」的十几处重复实现统一到公共 hook，并把「失败类提示要常驻」的语义保住。
- 待办/笔记/摘要等本地库改为显式建立父目录，「读取失败」与「确实没有数据」现在可以区分。

### 修复（问答正确率与「只依据微信消息」的硬约束，2026-09-15）

- **回答接地核对（生成侧）**：新增确定性核对 —— 回答里的金额、日期、手机号/卡号这类
  「必须逐字来自原文」的值，会与**回答自己引用的那几条原文**逐项对照；对不上就带着
  「具体是哪几个值不对」回炉重写一次，重写仍无引用才不予采用。合计不会误伤：
  等于引用原文里 ≤3 笔金额相加（如 3500+3500=7000）即算有出处；「一共 3 笔」这类
  计数是模型可以数出来的，刻意不查。微信问答与会话内 AI 问答共用这一条链路。
- **回答里的日期不再靠猜**：送给模型的窗口逐行带上该条消息自己的日期（此前只有窗口头
  给出锚点那天的日期，跨天窗口里模型要自己对时，而提示词又要求「写绝对日期」）。
- **会话级问答不再串台**：实体通道（`who:`）此前没有带上会话范围，在「会话内 AI 面板」
  或用户选定某个会话提问时，会把这个人**在别的会话（如群聊）里的发言**也召回进来当证据。
  现已随范围一起下发，检索到的证据全部落在所问会话内。
- **点名识别不再只依赖大模型**：规划器偶尔会漏掉问题里明确点到的人；现在把本地联系人
  昵称/备注与会话标题作为已知实体喂给检索层，实体路由与实体召回归位确定。
- **规划器短语按整词检索**：「收到转账」这类关键词此前被拆成 bigram（收到/到转/转账，
  其中「到转」是跨词噪音）；现在原样保留一份，检索侧按连续短语匹配，排序更准。
- 检索词里的数字只会被「原文中同样位置的数字」认领：日期与时钟里的数字不再被当成
  金额出处（否则「2026 元」会被原文里的「2026-09-05」放过）。
- 检索评估集与真实 FTS 对齐：合成评测的稀疏通道改为按**连续短语**语义判定命中，
  避免把「短语项命中不了」误判成算法缺陷。
