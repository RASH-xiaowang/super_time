# 变更记录

本文件记录所有**对外可见**的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 版本策略

- **唯一版本来源**是 `package.json` 的 `version`；`main.js` 读它作为 `APP_VERSION`，
  授权校验与安装器都用同一个值。
- 每次发布：先改 `package.json` → 在本文件顶部补一段 → `npm run dist` 出安装器。
- 材料性变更（新增出网点、扩大数据读取/扫描范围、改变数据用途）除了升版本，
  还要提升 `src/client/ui-app/privacy/consent.ts` 的 `PRIVACY_VERSION`，
  让用户在下次启动时重新同意 [`docs/PRIVACY.md`](docs/PRIVACY.md)。
- 逐条实施状态与验收证据不在本文件里，在 [`docs/RELEASE-PLAN.md`](docs/RELEASE-PLAN.md)。

## [1.0.2] - 2026-09-15

### 修复

- 修复在启动引导的授权验证步骤导入许可证后，点击「进入系统」没有反应、必须重启应用的问题；完成后会立即刷新授权状态并由原有授权门禁兜底。
- 修正启动引导页仍显示 `1.0.0` 的写死版本号，改为与安装包一致的 `1.0.2`。

### 调整

- 更新与许可证到期提醒现在覆盖启动引导、授权解锁和隐私同意页面；停留在首启流程中也能及时看到新版本。
- 在没有设置弹窗的首启页面移除无效的「去软件授权」动作，保留仍可用的导出激活请求等操作。

## [1.0.1] - 2026-09-15

### 新增

- 新增首次配置向导：把账号检测、数据库密钥、图片密钥与图片解码收成 5 步，并支持从第一个未完成步骤继续配置。
- AI 模型目录新增 MiMo（小米 Token Plan），支持已确认的 `mimo-v2.5` 模型。

### 调整

- 重构设置弹窗与旁栏导航：按配置、智能与隐私、授权与更新、维护自检、高级分组，降低首次使用和理解门槛。
- 将隐私体检与操作日志保留为主界面数据视图，配置、授权和维护动作继续收在设置弹窗中。
- 更新界面细节、健康页与提醒卡片样式，并同步调整 UI 验收、SSR 冒烟和前端说明。
- 轮换客户端许可证公钥，配合新的许可证签发材料。

## [Unreleased]

### 说明：尚不具备公开发布条件

上线门禁（`docs/RELEASE-PLAN.md` 第十二节）仍有未闭环项，**明确阻塞公开发布**的有：

- 随包资产许可未明（微信表情原图、朋友圈视频解密 WASM）——待法务结论；
- 历史提交中的真实密钥需轮换并重写 git 历史（仓库侧已出库，人工动作待执行）；
- 尚无出网总闸门：AI 之外的出网点（头像、远程图片/视频、地图底图、语音模型下载）目前无内置开关。

### 新增

- 本机微信数据工作台：会话与消息检索、全库统一搜索、统计与总览、联系人 360°、群聊洞察、
  朋友圈洞察、资金账本、数据健康、备份管家、隐私体检与敏感信息扫描、导出（CSV/HTML/TXT/XLSX/ZIP）。
- AI 能力：微信问答（稀疏 + 稠密检索）、每日总结、周期总结、群总结任务，
  统一经过隐私闸门（出站拦截 / 敏感字段打码 / 出站审计）。
- 社交图谱与知识图谱两个并列入口；知识笔记（`[[链接]]`、未解析目标成占位节点）与问答沉淀。
- 授权体系：设备指纹 + Ed25519 签名许可证，未授权时业务方法一律拒绝。
- 可靠性：后端进程独立运行并带超时与崩溃自愈（指数退避重启 ≤3 次）、
  导出与检索的流式化（内存有上界）、诊断日志落盘与导出。
- 安全基线：渲染进程沙箱、`shell.openExternal` 白名单（仅 http(s)、拒绝带凭据 URL）、
  导航守卫、CSP 收紧、asar 完整性等 fuses、打包态剥离调试启动开关。
- 工程化：vitest 单测 + windows-latest CI（含 bundle 与类型声明的一致性门禁）、
  打包冒烟、多条安全/功能冒烟脚本。
- 文档：根 `README.md`、`LICENSE`（MIT）、本变更记录、[`docs/PRIVACY.md`](docs/PRIVACY.md)、
  由 `gateway.ts` 自动生成的 [`docs/API.md`](docs/API.md)。

### 修复（相对首轮审查，见 RELEASE-PLAN 的条目）

- 授权闸门异常时改为**拒绝**而非放行（原先许可文件损坏会无证放行）。
- 后端调用加超时、worker 崩溃自动重启（原先卡死会无限转圈、进程死掉后功能整体失效）。
- 导出与搜索不再全表物化（原先最坏情况常驻数 GB 内存并阻塞事件循环）。
- 配置写入改为原子写，损坏文件先备份再覆盖（原先会被默认值无声覆盖）。
- 密钥不再镜像进 `config.json`，改存权限收紧的 `secrets.json`；开发态迁移不再携带数据源与密钥
  （原先会把真实微信库静默解密到新的状态目录）。
- 打包版语音 silk 解码不可用（可执行文件落在 asar 内无法 spawn）。
- 干净检出打包会打出没有前端的包（`pack`/`dist` 现前置 `build:ui`）。
- 主进程未声明 `AppUserModelID`，任务栏固定/分组可能认成两个应用。
- 「自动获取原图（CDN）」与「原图解密方式」两个开关此前只存不用（关掉后取图路径照旧出网，
  界面上却显示「已关闭」）；现在关闭后后端不再发起取回请求，且与 AI 出网开关相互独立。
- 设置里的「HTTP API 启用 / 访问令牌 / 监听端口」三个开关没有对应服务端，已从界面撤下
  （避免让用户以为关掉端口就等于关掉了外部访问）。
- 向量索引构建不再与并发调用互相撞锁：写事务不再跨网络保持；同一向量库上的并发调用会合并到
  同一轮构建；重建中途失败也不会把已有向量清空（删除动作随事务一起回滚）。
- 建向量索引与若干统计面板不再把整张消息表读进内存（改为逐行游标读取）：
  20 万行语料下构建期内存峰值从 +340MB 降到 +103MB，且余量只随「单次构建上限」变化。
- 一次性数据源引导（bootstrap）不再只拷 SQLite 主库：现在连 `-wal` 一起拷（跳过 `-shm`），
  避免在 WAL 模式下把「上一代」索引静默拷到新数据根。
- 成员搜索与索引重建不再互相撞锁：索引库的写入统一走一个写闸，构建在飞时显式降级并留痕。
- LLM 之外的出网点（公众号封面、图片、朋友圈视频、whisper 引擎/模型下载）也走重试；
  模型下载支持断点续传，并修掉「截断的下载被当成完整模型」这个缺陷。
- 向量索引状态查询不再每次重开连接并 `COUNT(*)`（改为指纹缓存 + `meta.rows`，热路径 0 SQL）；
  每日摘要的到期任务改为有界并发；顺带修掉「从未手动运行过的任务永不触发」的调度缺陷。
- 打包内容改为白名单 + 体积预算（`src/` 下只允许已知子树），构建日志里的体积统计不再恒报 0KB。
- 一批前端正确性与性能修正：反馈按钮的并发闸门、快照监听者 Map 泄漏、分页 `hasMore` 判据、
  图谱小地图的逐帧重算、世界地图的 memo、朋友圈文本解析的平方项（2.9MB 语料 12.97s → 2.88ms）。
- 测试与脚本不再写仓库：`license-smoke` 改为注入测试公钥，视觉审计脚本收回 `tools/visual-audit/`
  （并从源码里移除硬编码的真实群名）。
- 导出与备份改为条目级流式写入：xlsx 导出 10 万行的内存峰值从 +221.6MB 降到 +26.5MB，
  导出过程支持取消（取消后不留半成品），备份在部分失败时如实报错而不是「报成功」。
- 图片列表取图改为批量查询：一屏 30 张图只查一次库（原先逐张 30 次）。
- 反馈提交加了去重（同一轮 10 秒内重复提交只生效一次）。
- 首启的三道闸门（引导/授权/隐私同意）新增**仅非打包态生效**的调试豁免开关，
  供 UI 自动化使用；打包版即便设了环境变量也不会被豁免。
- 「提示语 N 秒后消失」的十几处重复实现统一到公共 hook，并把「失败类提示要常驻」的语义保住。
- 待办/笔记/摘要等本地库改为显式建立父目录，「读取失败」与「确实没有数据」现在可以区分。
