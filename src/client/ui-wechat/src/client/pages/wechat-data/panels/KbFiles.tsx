/**
 * 知识库「文件」视图 —— 合并外壳（MergedSections）里的第三个分段（tab: `kbfiles`）。
 *
 * 与「笔记库」「知识图谱」是同一份**作用域**（当前知识库）下的第三类内容：
 *   · 笔记库 / 知识图谱：看的是 `wechat_notes.db` 的 notes 表；
 *   · 文件（本文件）：看的是 `wechat_kb_files.db` 的 kb_files 表 —— 用户从自己电脑上
 *     **登记**进来的文件（txt / md / csv / json / html …），后端解析成正文、切块、
 *     进关键词索引（可选再出网做向量），于是「我手边这份合同 / 这堆日志」也能被问答检索到。
 *
 * 三条不可动摇的纪律（都来自 T2 的验收口径，写在这里防止后人「顺手简化」）：
 *   ① **原文件只读**：登记只记路径与内容指纹，后端解析的是 blob 副本。所以
 *      「从知识库删除」永远不碰用户电脑上的那份原文件 —— 界面上也必须这么说。
 *   ② **`unsupported` ≠ `failed`，且任何一处都不许说「文件损坏」**：前者是本机还没有
 *      对应的解析器（等版本升级就能解），后者是解析器存在但真的抛了（要用户换文件）。
 *      两者的排查方向完全相反。
 *   ③ **`readError` 与「确无文件」必须分开说**：读不到库时后端仍返回空列表，
 *      没有 `readError` 这两个状态在界面上长得一模一样（N1 那条纪律）。
 *
 * 作用域：每个请求都带 `kbId`（必填位置参数）。文件 id 是**全局自增**的，所以
 * `kbId` 在删除 / 开关上是**守卫** —— 拿甲库的 id 调乙库会物理删掉甲库那一条。
 */

export * from './kb-files-support.tsx'
export * from './kb-files-panel.tsx'
export * from './kb-files-detail.tsx'
